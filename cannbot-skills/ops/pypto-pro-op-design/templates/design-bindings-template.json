{
  "schema_version": 1,
  "bindings": [
    {
      "class_id": "{class_id}",
      "selection_field": "optional_patterns",
      "reference": "{exact path from KB_SELECTION.json}",
      "selection_reason": "{reason from KB_SELECTION.json}",
      "requirements": [
        {
          "req_id": "R01",
          "kind": "obligation",
          "source_anchors": [
            "{heading path > item label}"
          ],
          "status": "applies",
          "class_evidence": "{SPEC/shape/dtype/target fact proving applicability}",
          "invariant": "{concrete class-specific invariant}",
          "planned_location": "{DESIGN decision + final test_<op>.py file/symbol; staged location may be additional}",
          "verification_method": "{static, runtime, or profiling check}"
        }
      ]
    }
  ]
}
