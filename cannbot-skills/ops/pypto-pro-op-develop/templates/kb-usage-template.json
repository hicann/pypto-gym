{
  "schema_version": "{replace with the integer topology-map.json contract.contract_version}",
  "op": "{op from KB_SELECTION.json}",
  "class_id": "{class_id from KB_SELECTION.json}",
  "invariants": [
    {
      "reference": "{exact selected path}",
      "invariant": "[{selection_field}:{req_id encoded with json.dumps(..., ensure_ascii=False)} | {source_anchors encoded with json.dumps(..., ensure_ascii=False)}] {Binding invariant copied verbatim}",
      "implementation": {
        "file": "{real final or staged implementation file}",
        "symbol": "{real symbol in that file}",
        "status": "implemented"
      }
    }
  ]
}
