---
schema_version: 1
op_name: <op>
dynamic_axes:
  - <实际动态轴>
---

# <op> 算子设计

## 算子契约

- 规格与参考计算：<SPEC、golden 的链接，入口及必要参数>。
- 目标环境：<设备与 PyPTO 版本>。
- 数学定义：<公式、输入限制、特殊情况及输出含义>。
- 精度要求：<SPEC 中的比较方式与容差，按输出或 dtype 区分>。

| 名称 | 输入 / 输出 | shape | dtype | 说明 |
|---|---|---|---|---|
| <张量> | <方向> | <shape> | <dtype> | <含义与限制> |

| 动态轴 | 对应输入与维度 | 支持范围 | 动态标注与循环 |
|---|---|---|---|
| <实际动态轴> | <位置> | <范围> | <实现表达方式> |

## 计算图与 API 映射

| 计算步骤 | 输入（名称、shape、dtype） | 计算 / API | 输出（名称、shape、dtype） | 依据 |
|---|---|---|---|---|
| <步骤> | <输入> | <公式 / API> | <输出> | <资料链接> |

- 类型转换：<位置、源类型、目标类型及原因；已在表中明确时省略>。
- 数据依赖：<图或简要说明；已由表格完整表达时省略>。

## 分解与模块边界

**Decision: `module_count = <N>`**

`decomposition_level: <L0 | L1>`

- 划分依据：<估算参考、实际选择及调整原因>。
- 模块接口：[module_interfaces.yaml](eval/module_interfaces.yaml)

| 模块 | 负责计算 | 输入 | 输出 | 划分原因 |
|---|---|---|---|---|
| <模块> | <职责> | <张量> | <张量> | <原因> |

## 范式与设计决策

### SK/AT 选择

- 所选模式：<SK/AT 名称与链接，或不采用的原因>。
- 选择依据：<结构与适用条件的对应关系，必要的替代方案比较>。

### Tiling 与数据切分

| 计算段 | 数据块 shape / 切分轴 | Cube / Vector TileShape | 设置位置 | 资源估算及依据 |
|---|---|---|---|---|
| <计算> | <shape / axis> | <实际值> | <操作之前> | <驻留张量、缓冲副本、字节数及可用容量> |

| 参数 | 当前值 | 必须满足的条件 | 可调整范围或候选 | 依据 |
|---|---|---|---|---|
| <参数> | <值> | <限制> | <候选或固定原因> | <资料链接> |

- 尾块：<有效长度、填充或掩码、写回范围的具体处理>。

### 实现配置

<显式配置如下；无覆盖时注明使用目标版本默认值。>

| 配置 | 选定值 | 用途与选择依据 | 验证情况 |
|---|---|---|---|
| <pass_options / runtime_options 参数> | <值> | <具体原因及资料链接> | <实际依据或待验证事项> |

### 布局与量化（按需）

- 布局变化：<源与目标 shape / 布局、转换位置、是否共享存储或原地修改>。
- 数据访问：<连续、分页或稀疏访问方式，索引来源与无效位置处理>。
- 量化：<转换位置、格式、scale 的 shape/dtype 及对应轴、乘除约定>。
- 数值边界：<舍入、饱和、全零输入或零 scale 的处理>。

### 循环与状态

| 循环 | 范围与步长 | 表达方式 / 展开选择 | 迭代之间的依赖 |
|---|---|---|---|
| <名称或轴> | <边界及来源> | <方式及参数> | <独立或依赖状态> |

| 状态 | shape / dtype | 初始化位置与值 | 每轮如何更新 | 最终使用或写回时机 |
|---|---|---|---|---|
| <名称；无跨迭代状态时省略> | <类型> | <作用域及初始值> | <更新公式> | <条件或位置> |

### 数值稳定性（按需）

| 风险 | 处理方法与累加 dtype | 依据 | 待验证事项 |
|---|---|---|---|
| <实际数值风险> | <所选方法> | <推导或参考计算> | <验证要求> |

## 伪代码

```text
<入口与输入输出>
<预处理及选定配置>
<数据读取、完整计算、必要的循环与状态更新>
<后处理与全部输出写回>
```

## 验证方案 / Verification Plan

### 设计检查结果

| 检查项 | 当前结论 | 依据或待办 |
|---|---|---|
| <实际检查的项目> | <具体结论 / 待检查> | <资料、推导、工具结果或下一步> |

### 实现后的验证方案

| 场景 | 输入配置 | 比较方式与通过标准 |
|---|---|---|
| <典型、动态范围端点、非整块输入或数值边界> | <shape、dtype、参数和值分布> | <参考输出及精度要求> |

## 风险与开放问题

| 类型 | 内容与影响 | 处理 |
|---|---|---|
| <风险 / 开放问题；无则注明无> | <具体内容> | <下一步> |
