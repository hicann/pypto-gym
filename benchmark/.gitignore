# 上游 KernelBench 下载缓存 (由 scripts/download_kernelbench.sh 拉取)
.cache/

# Python 字节码
__pycache__/
*.py[cod]

# 本地 benchmark 配置副本
configs/local_*.yaml

# 测试产物 (skill_validate 结果) — 避免误提交
custom/*/.skill_validate/
